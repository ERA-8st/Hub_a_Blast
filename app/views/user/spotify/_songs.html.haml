%h3 SONGs
.row
  - if searchsongs.present?
    - n = 0
    - searchsongs.first(song_count).each do |searchsong|
      .col-lg-3.col-md-3.col-xs-3
        - n += 1
        = set_song_image(searchsong,2,true,false)
        %br
        %p{id: "song_#{n}"}= searchsong.name
- if searchsongs.count > song_count
  - song_count += 8
  = link_to "更に表示する", user_spotify_index_path(song_count: song_count, search: search_word), id: "more_songs", remote: true