.container
  %h3.text-center 通知
  -# = link_to destroy_all_users_notifications_path, method: :delete do
  -#   %i.fas.fa-trash{ style = "color: black;"}
  -#   %h7{ style = "color: black;"} 全削除
  %hr
  .row
    .col-lg-3.col-md-3.col-xs-3
    .col-lg-6.col-md-6.col-xs-6
      - if @notifications.exists?
        .users-index.text-center
          %table.table= render @notifications
      - else
        %p 通知はありません